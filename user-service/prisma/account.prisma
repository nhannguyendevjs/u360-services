model Account {
  id       Int    @id @default(autoincrement())
  username String
  password Json
  user     User   @relation(fields: [userId], references: [id])
  userId   Int    @unique

  @@map("accounts")
}
